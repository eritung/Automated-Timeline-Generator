# 製作時程排程工具（Streamlit）

## 啟動方式
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## 這版更新
- 嘗試隱藏流程設定最左側多餘勾選欄
- 新增「快速隱藏提供素材」按鈕
- 各區塊改成淡白色墊底感
