# 製作時程排程工具（Streamlit）

## 啟動方式
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## 這版更新
- 流程設定以直接新增、刪除、修改任務為主
- 移除批次貼上區塊
- 移除多餘的說明文字
- Excel 輸出邏輯維持不變
