# 製作時程排程工具（Streamlit）

## 啟動方式
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## 這版更新
- 排程預覽改成簡化時程圖樣式
- 下載 Excel 按鈕維持在預覽區右上
- 主按鈕紅色調整為較柔和版本
- 專案設定重新排序，版面更整齊
