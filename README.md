# 製作時程排程工具（Streamlit）

## 啟動方式
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## 這版更新
- 拿掉預設專案名稱，進入頁面時不再顯示 DOVE
- 新增「顯示」欄位，可直接勾選/取消來隱藏任務，例如提供素材
- 排程方式文案改為：製作日推進 / 上線日回推
- 移除指定的預覽文案
