# 製作時程排程工具（Streamlit）

## 啟動方式
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## 這版更新
- 流程列上下間距縮小
- 複製按鈕改為 icon 樣式
- 斑馬紋底色加深，閱讀更清楚
